version: "3"

networks:
  jaeger:

services:
  app:
    build:
      context: .
      dockerfile: Unit.dockerfile
    environment:
      JAEGER_ENDPOINT: http://jaeger:4317
    networks:
      - jaeger
