version: '3.9'

networks:
  k6:
  grafana:
  monitoring:

volumes:
  spark_get_influxdb_data:

services:
  spark_get:
#    deploy:
#      resources:
        # limits:
          # memory: 1G
          # cpus: "0.25"

    build:
      context: ..
      dockerfile: spark.dockerfile
    networks:
      - k6
      - monitoring
    ports:
      - "7002:7000"
      - "7003:7001"
    environment:
      PORT: 7000
      METRIC_PORT: 7001

  spark_get_influxdb:
    image: influxdb:1.8
    networks:
      - k6
      - grafana
    environment:
      - INFLUXDB_DB=k6
      - INFLUXDB_HTTP_BIND_ADDRESS=:8098
  
  spark_get_prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml 
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--web.listen-address=:9094"
    ports:
      - "9094:9094"
    networks:
      - monitoring

  k6_spark_get:
    image: grafana/k6:latest
    depends_on:
      - spark_get
    networks:
      - k6
    environment:
      - K6_OUT=influxdb=http://spark_get_influxdb:8098/k6
    volumes:
      - ./scripts:/scripts
    command: run /scripts/script_spark_get.js

  grafana_spark_get:
    image: grafana/grafana:9.3.8
    networks:
      - grafana
      - monitoring
    ports:
      - "3004:3004"
    environment:
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_BASIC_ENABLED=false
      - GF_SERVER_HTTP_PORT=3004
    volumes:
      - ./provisioning:/etc/grafana/provisioning
      - ./dashboards:/var/lib/grafana/dashboards
